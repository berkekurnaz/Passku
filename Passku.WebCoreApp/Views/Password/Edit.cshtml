@model Passku.Models.Concrete.StoredPassword
@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <strong class="card-title">Update Password</strong>
            </div>
            <div class="card-body">
                <div id="pay-invoice">
                    <div class="card-body">
                        <form asp-controller="Password" asp-action="Edit" method="post" enctype="multipart/form-data">

                            <div class="form-group">
                                <label class="control-label mb-1">Title</label>
                                <input asp-for="Title" type="text" class="form-control" value="@Model.Title" required>
                            </div>

                            <div class="form-group">
                                <label class="control-label mb-1">Password</label>
                                <input asp-for="Password" type="text" class="form-control" value="@Model.Password" required>
                            </div>

                            <div class="form-group">
                                <label class="control-label mb-1">Content</label>
                                <textarea asp-for="Content" class="form-control" rows="3">@Model.Content</textarea>
                            </div>

                            <div class="form-group">
                                <label class="control-label mb-1">Url</label>
                                <input asp-for="Url" type="text" class="form-control" value="@Model.Url" />
                            </div>

                            <div>
                                <button type="submit" class="btn btn-lg btn-success btn-block">
                                    <i class="fa fa-plus-pencil fa-lg"></i>&nbsp;
                                    <span id="payment-button-amount">Edit Password</span>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>

            </div>
        </div>

    </div>
</div>

